README for Blockchain folder.

To run and compile Blockchain.c compile normally for C program

$gcc Blockchain.c -o Blockchain
$./Blockchain

To compile and run Blockchain.py compile normally for Python program

$python Blockchain.py

To compile and run Blockchain.go there are two ways:

$go run Blockchain.go

or

$go build Blockchain.go
$./Blockchain

To compile and run main.rs:

$cargo run

Note: since using makefile for, the actual rust code "main.rs" is inside the folder src; however, we included a copy of it out of src folder for visibility.